# ── API publique (activer quand nécessaire) ───────────────────────────────────
# Pour l'instant l'API reste interne (accessible uniquement via le proxy nginx
# du container frontend). Décommenter et activer ce fichier si l'API doit être
# exposée publiquement sur api.labmedis-togo.com.

# server {
#     listen 80;
#     listen [::]:80;
#     server_name api.labmedis-togo.com;
#
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }
#
# server {
#     listen 443 ssl;
#     listen [::]:443 ssl;
#     server_name api.labmedis-togo.com;
#
#     ssl_certificate     /etc/letsencrypt/live/api.labmedis-togo.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.labmedis-togo.com/privkey.pem;
#     include             /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;
#
#     client_max_body_size 25M;
#
#     location / {
#         proxy_pass         http://localhost:8082;
#         proxy_http_version 1.1;
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#         proxy_read_timeout 120s;
#     }
# }
